syntax = "proto3";

package aveplen.auth;
option go_package = "github.com/aveplen-back/auth";

service Authentication {
    rpc GetNextSynPackage(SynPackage) returns (SynPackage);
}

message SynPackage {
    uint64 id = 1;
    bytes contents = 2;
}
